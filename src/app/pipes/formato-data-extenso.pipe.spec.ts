import { FormatoDataExtensoPipe } from './formato-data-extenso.pipe';

describe('FormatoDataExtensoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoDataExtensoPipe();
    expect(pipe).toBeTruthy();
  });
});
