import { TranslateContractStatusPipe } from './translate-contract-status.pipe';

describe('TranslateContractStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateContractStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
