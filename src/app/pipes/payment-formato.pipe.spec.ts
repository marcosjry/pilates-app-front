import { PaymentFormatoPipe } from './payment-formato.pipe';

describe('PaymentFormatoPipe', () => {
  it('create an instance', () => {
    const pipe = new PaymentFormatoPipe();
    expect(pipe).toBeTruthy();
  });
});
